TODO
====

Base
----
+	Fix auto loader
+	Add locking mechanism (semaphores) for the shared memory object
-	Refine timing from seconds to millisecond precision.
-	Change user/group of main daemon
-	Verbose log levels
-	Read config/log file arguments
-	Read tasks dir(s) from config file


Managers
--------
-	Rewrite cron manager: the wait until function could be rewritten using the 
	php time_sleep_untill function.
-	Rewrite forked manager (fill up to x forks and fork again when the first one 
	has finished.
-	Extend gearman manager from the forked manager so it can re-use the ability
	to run multiple gearman worker instances. Currently only a single gearman
	worker is started. 

General
-------
-	Write howto use PhpTaskDaemon
-	Create simple website for the project